export const PROJECT_NAME = 'notepad-web'

export const DOCUMENT_DATABASE_NAME = PROJECT_NAME
export const ACTIVE_DOCUMENT_STORAGE_KEY = `${PROJECT_NAME}:active-document`
export const THEME_STORAGE_KEY = `${PROJECT_NAME}:theme`
export const FONT_STORAGE_KEY = `${PROJECT_NAME}:font`
export const SYNC_CHANNEL_NAME = `${PROJECT_NAME}:sync`
export const SYNC_FALLBACK_STORAGE_KEY = `${PROJECT_NAME}:sync-event`
